from librerias.arith import addere as add ##Solo incluyes una de las funciones de la libreria y le podemos cambiar el nombre
from librerias import arith, logic ## importammos varias librerias a la vez

add(1,2)